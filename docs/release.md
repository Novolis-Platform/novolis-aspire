# Release

## Versioning

Follows `build/version.json` and Novolis shared versioning targets.

## Publishing

Pack **Novolis.Aspire.Hosting.Signoz** to GitHub Packages after `dotnet build` on `Novolis.Aspire.slnx` succeeds.

## Contents

NuGet package includes hosting extensions, embedded SigNoz asset files, XML documentation, and the package README.
